const scroll = new LocomotiveScroll({
  el: document.querySelector('[data-scroll-container]'),  // Correctly target the scroll container
  smooth: true,
  direction: 'horizontal'  // Set the scroll direction to horizontal
});
